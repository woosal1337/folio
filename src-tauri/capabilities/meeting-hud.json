{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "meeting-hud",
  "description": "Meeting-detection HUD popover (GET-143). Frameless always-on-top window that surfaces a detected call. Minimal surface: it only invokes the app-defined meeting commands and listens for the meeting-detected event, both covered by core:default. No fs / opener / dialog grants — it has no need for them.",
  "windows": ["meeting-hud"],
  "permissions": [
    "core:default",
    "core:webview:default",
    "core:app:default",
    "core:window:allow-close"
  ]
}
